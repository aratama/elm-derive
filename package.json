{
  "name": "elm-derive",
  "version": "1.0.0",
  "description": "WIP: JSON Encoder/Decoder code generator for Elm",
  "main": "",
  "bin": {
    "elm-derive": "./bin/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd cli && npm run build && cd ../web && npm run build && cd ../example && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aratama/elm-derive.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/aratama/elm-derive/issues"
  },
  "homepage": "https://github.com/aratama/elm-derive#readme",
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.1-3"
  }
}
