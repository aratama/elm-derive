{
  "name": "elm-autoencoder-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "elm make src/Main.elm  --optimize --output=./dist/elm-autoencoder.js && esbuild ./src/index.mjs --bundle --outfile=../bin/index.js --platform=node",
    "start": "node ../bin/index.js example/TodoList.elm"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-elm-compiler": "^5.0.4"
  },
  "dependencies": {
    "esbuild": "^0.3.5",
    "fs-extra": "^9.0.0"
  }
}
